# Refactor Summary — testStrategyRepository

## Capabilities refactored

1. **Framework detection** (VIOLATION 2-A — business logic in repository)
2. **TsAnalyzer instantiation** (VIOLATION 2-B — infrastructure bypassing DI)

## Files modified

### `src/repositories/ports/testStrategy.ts`
- Removed `detectFramework(targetFilePath: string): TestFramework` from `ITestStrategyRepository`
- Removed `TestFramework` import (no longer needed at repository layer)
- Added `readPackageDeps(targetFilePath: string): Record<string, string> | null`

### `src/repositories/testStrategyRepository.ts`
- Removed standalone `parseFunctionsFromFile` (was instantiating `TsAnalyzer` ad-hoc)
- Added `constructor(private readonly tsAnalyzer: TsAnalyzer)` — DI injection
- `parseFunctions` class method now calls `this.tsAnalyzer.getSourceFileIfExists` directly
- Replaced standalone `detectFramework` with `readPackageDeps` — returns merged `dependencies + devDependencies` or `null`; no framework decision
- `detectFramework` class method replaced with `readPackageDeps` delegation

### `src/domains/testStrategy.ts`
- Added private `detectFramework(deps: Record<string, string> | null): TestFramework` — owns the `'vitest' in deps` business rule; defaults to `'jest'`
- `analyze()` now calls `this.detectFramework(this.repo.readPackageDeps(targetFilePath))` instead of `this.repo.detectFramework(targetFilePath)`

### `src/core/di/setup.ts`
- Added `import { TsAnalyzer } from '@src/infrastructures/tsAnalyzer'`
- `buildRepos`: `new TestStrategyRepository(new TsAnalyzer({ skipAddingFilesFromTsConfig: true }))`

### `src/repositories/parsers/__tests__/tsParser.test.ts`
- Replaced `detectFramework` import with `readPackageDeps`
- Rewrote `describe('detectFramework')` → `describe('readPackageDeps')`: tests now assert on the returned deps object (`Record<string, string> | null`) rather than a `TestFramework` string

## DI tokens added
None — `TsAnalyzer` is constructed inline inside `buildRepos` and passed to `TestStrategyRepository`; no container token needed.
